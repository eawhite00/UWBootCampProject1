console.log("table-events.js is connected to the page")

var destinationName = $("#destination-name").val().trim();
var destinationState = $("#destination-state").val().trim();
var duration = $("#duration-value").val().trim();
